<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @event.title %>
</p>

<p>
  <b>Date:</b>
  <%= @event.date.to_formatted_s(:day_month_day) %> at <%= @event.date.to_formatted_s(:time_of_day) %>
</p>

<p>
  <b>Venue:</b>   	 
  <%= @venue != nil ? link_to(@venue.name, @venue) : "unknown" %>
</p>


<%= link_to 'Edit this event', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
